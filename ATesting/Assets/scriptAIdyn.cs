using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scriptAIdyn : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.log("hi this is aidyn!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
